# Handoff — Bug #5 + Bug #4b + commit wave (session 2026-06-20 session-4 EOD)

**Written:** end of session-4 that (1) implemented + verified the **Bug #5 fix (v0.8.8)** with two-stage containment (substring filter + per-process re-entry counter), (2) committed the accumulated v0.8.6/v0.8.7/v0.8.8 wave + scaffold + docs as 4 atomic commits, (3) attempted **Bug #4b** and shipped it as **PARTIAL fix (v0.8.9)** after live probes revealed the prior doc diagnosis was wrong (namedComponents is empty for ExtendedSimpleDialog, not "wrong aspect names"), and (4) committed the v0.8.9 wave as 2 more commits.
**For:** the next session — push the 11 local-only commits when ready, optionally pick up Bug #4b view-tree archaeology (1-2 hours) OR Bug #2 deep dive (dedicated multi-hour session per session-3 handoff).
**Supersedes:** nothing — this is a NEW handoff file for the 2026-06-20 date. The prior session ([HANDOFF-2026-06-19.md](./HANDOFF-2026-06-19.md), committed at a434f69) is the session-3 EOD reference.

---

## User direction in this session (session-4, condensed)

- Open with: "read and understand the session handoff" — read HANDOFF-2026-06-19.md before any work.
- "check with wsl if changes are commited, then we can fix the bugs" — verify git state first, then pick a bug.
- Picked **Bug #5** (image-killer, recommended) when offered the choice of #5 / #2 / #4b / all-three.
- "I will check the file in" — confirmed user would handle the first workspace file-in (bridge was DOWN at session start).
- "Try: curl http://127.0.0.1:9876/health  Token written to .token: 3959408417191-443201  VWBridge: listening via SocketAccessor" — pasted Transcript output confirming user filed-in v0.8.8.
- "commit using wsl then move on to the next bug" — explicit commit verb + continuation directive.
- Picked **Bug #4b** (polish, recommended) when offered the choice of #4b / #2 / both / push-first.
- "you can just use powershell" — corrected agent's WSL escaping nightmare; switched to PowerShell + curl.exe.
- Explicit handoff request via question tool: "Wrap session: handoff + push (Recommended)" — this document + push.

---

## Work completed in session-4

### Phase 1 — Bug #5 fix (v0.8.8) — image-killer contained

**Two-stage defense in [`VWBridge.st`](../src/vw-bridge/VWBridge.st):**

- **Stage 1** in `handleEvalBody:`: new helper `looksLikeRecursiveDispatch:` rejects bodies containing BOTH `'VWBridge'` AND `'dispatch'` substrings (case-sensitive). Pre-compile gate. Returns `400 recursive_dispatch_forbidden`. Allows the documented workaround #1 (`VWBridge singleton handleWindows` — no `'dispatch'` substring).
- **Stage 2** in `dispatch:headers:body:`: original method became a guard wrapper around renamed `doDispatch:headers:body:`. Two new class instance variables (`dispatchDepthByProcess` IdentityDictionary + `dispatchDepthMutex` Semaphore), class-side mutex-protected helpers `incrementDispatchDepthFor:` / `decrementDispatchDepthFor:`. Runtime gate via per-process re-entry counter; `depth > 1` → `400 recursive_dispatch` with the depth field. Balanced by `ensure:` on the happy path. Catches indirect paths Stage 1 misses (constructed selectors, `perform:` with split strings).

**Version bumped 0.8.5 → 0.8.8 at the 3 standard sites** (header, dispatcher comment, `/health` body). Note: v0.8.6 (Bug #4 fix) + v0.8.7 (Bug #1 fix) from session-3 were never committed as separate file states, so the disk diff jumps directly to v0.8.8.

**Verification — 6 curl probes, all green:**

| Probe | Result | Time |
|---|---|---|
| `GET /health` (pre-check) | `{"status":"ok","version":"0.8.8"}` | — |
| `GET /windows` (auth) | JSON array of 4 windows | — |
| Stage 1 literal recursive | HTTP 400 `recursive_dispatch_forbidden` | **3.6 ms** |
| Stage 2 constructed selector | HTTP 200 wrapping inner 400 `"depth":2` | **16.3 ms** |
| Legit `VWBridge singleton token` | Returns the token literal | 2.7 ms |
| Post-abuse `GET /health` | `{"status":"ok","version":"0.8.8"}` — bridge alive | — |

The session-3 wedge pattern (which required `vwnt.exe` restart) now returns 400 in ≤16 ms with the bridge unaffected.

### Phase 2 — Commit wave 1 (4 commits for session-3 + this-session-Bug-#5 work)

| Hash | Subject | Files |
|---|---|---|
| `c039817` | feat: VWBridge v0.8.8 - fix Bug #1 + Bug #4 + Bug #5 | `src/vw-bridge/VWBridge.st` |
| `162e60c` | test: add VWBridgeTest SUnit scaffold for v0.8.8 bridge | `src/vw-bridge/VWBridge-Test.st` |
| `d68470f` | docs: mark Bug #1, #4, #5 FIXED; soften Bug #5 anti-pattern | `knowledge/vw-bridge-known-issues.md`, `knowledge/vw-bridge-testing.md` |
| `a434f69` | docs: HANDOFF-2026-06-19 session-3 EOD update | `knowledge/HANDOFF-2026-06-19.md` |

Style detection: SEMANTIC prefix (matching repo's 16/18 = 89% convention). Extended body sections matching f10101c's style. No co-author footer (matches repo).

### Phase 3 — Bug #4b attempted, PARTIAL fix shipped as v0.8.9

**Investigation (probes via /eval — first agent-driven reload since Bug #5 fix):**

- `ExtendedSimpleDialog instVarNames` → `#()` (prior doc claim of extra inst vars was wrong)
- Superchain: `ExtendedSimpleDialog → SimpleDialog → ApplicationModel → Model → Object`
- 9 override methods including `builder`, `allButOpenFrom:` (delegates `buildDialogSubCanvasWith:` to source)
- 47 SimpleDialog subclasses in this image total
- Live Help dialog (`ExtendedSimpleDialog` from PartySearchView): `builder namedComponents` **EMPTY (`#()`)**
- Source's builder is the SAME object as dialog's builder (`srcBuilderSameAsDialog: true`) — also empty
- Source class: `QueriesHelpView`; its inst vars don't include direct widget references

**The doc's prior diagnosis was wrong.** It speculated ExtendedSimpleDialog has extra instVars and its own builder layout. Reality: ExtendedSimpleDialog has no extra instVars, shares the dialog's builder, and the builder's namedComponents is empty regardless of which aspect names we probe. The widgets must live elsewhere (probably built dynamically into the view tree via `dialogSubSpecSymbol`-resolved spec — needs view-tree walking to find).

**What I shipped (partial fix in v0.8.9):**

1. **New `class` field on `describeDialog:` response** — callers see `"class":"ExtendedSimpleDialog"` (or whichever of 47 subclasses) without round-tripping `/eval`. Backwards-compat addition.
2. **`describeDialog:` refactored** into orchestrator + 2 helpers:
   - `extractDialogMessageFrom:` tries common aspects (`#displayMessageString`, `#message`, `#text`, `#displayText`, `#labelString`, `#messageText`)
   - `extractDialogButtonsFrom:` iterates ALL widgets, matches `*Button*` spec class substring
3. Defense-in-depth for any future subclass that DOES register aspects — but does NOT fix ExtendedSimpleDialog specifically (its `namedComponents` is empty regardless of aspect names).

**Verified live**: `/dialogs` now returns `{"class":"ExtendedSimpleDialog","message":null,"id":"992572","buttons":[]}` for the Help modal. The `class` field works; message + buttons still null/empty (the deferred part).

**Meta-win**: this v0.8.9 reload was the FIRST agent-driven `/eval` file-in since Bug #5 was fixed in v0.8.8. Worked cleanly, no wedge. Going forward I can drive bridge reloads myself without the user touching a workspace.

### Phase 4 — Commit wave 2 (2 commits for v0.8.9 partial fix)

| Hash | Subject | Files |
|---|---|---|
| `a34bb13` | feat: VWBridge v0.8.9 - partial Bug #4b fix + class field | `src/vw-bridge/VWBridge.st`, `src/vw-bridge/VWBridge-Test.st` |
| `c71b2a9` | docs: Bug #4b - PARTIALLY FIXED in v0.8.9, investigation findings logged | `knowledge/vw-bridge-known-issues.md` |

Honest commit messages: code commit explicitly notes "partial improvement", "What this does NOT fix", and the deferred-fix path. Doc commit notes "prior diagnosis was speculation" with full probe-result table.

---

## Current state (end of session-4)

- **VW image:** still up. v0.8.9 loaded. Windows currently open: GbxVisualLauncher (GemStone Launcher), VisualLauncher (storedev64), WealthPublishedPundleVersionsTool (MAS Loaded Items), Workbook (Workspace), MasLauncher (MOMENTUM WEALTH - DEVELOPER --- Session Number: 4). Party Search was opened + closed cleanly during Bug #4b verification.
- **Bridge:** UP at v0.8.9 on `127.0.0.1:9876`. Token at [.token](../src/vw-bridge/.token) (regenerated by every `VWBridge start`). `Dialog useNativeDialogs: false` set this session — persists across bridge re-file-ins, does NOT persist across `vwnt.exe` restart.
- **Bridge code on disk:** v0.8.9 in [`VWBridge.st`](../src/vw-bridge/VWBridge.st) (60 KB, 1554 lines), matches what's running in the image.
- **SUnit scaffold on disk:** [`VWBridge-Test.st`](../src/vw-bridge/VWBridge-Test.st) version-locked to v0.8.9. NOT loaded in image yet this session — file-in via Workspace when needed.
- **Git:**
  - `main` at `c71b2a9` LOCALLY, **11 commits ahead of `origin/main`**. NOT pushed yet (user said push is part of this wrap-up; will push after this handoff commit).
  - Working tree CLEAN before this handoff edit; about to become +1 file (this handoff) → 12 commits ahead after the handoff commit lands.
- **Known bugs:**
  - **Bug #1: FIXED in v0.8.7** (verified session-3, committed this session)
  - **Bug #2: NOT FIXED.** Scope: only `#contractNumber` + `#surname` (both wrap in `Dialog confirm:`). Per session-3 handoff, "separate dedicated session — too deep to bundle".
  - **Bug #3: not a bug** (calibration only)
  - **Bug #4: FIXED in v0.8.6** (verified session-3, committed this session)
  - **Bug #4b: PARTIALLY FIXED in v0.8.9** — class field shipped, message + buttons extraction for ExtendedSimpleDialog deferred (requires view-tree archaeology, 1-2 hour session)
  - **Bug #5: FIXED in v0.8.8** (designed + verified + committed this session, 6 probes green)

---

## Pending tasks (next session)

### Immediate on resume

1. **Confirm bridge state:**
   ```powershell
   curl.exe -sS http://127.0.0.1:9876/health
   ```
   Should return `{"status":"ok","version":"0.8.9"}`. If not, file-in `VWBridge.st` — use `/eval` (no longer needs Workspace thanks to Bug #5 fix) OR Workspace as fallback:
   ```smalltalk
   'C:\Users\ammaganyane\tm\tm-context\src\vw-bridge\VWBridge.st' asFilename fileIn
   ```

2. **Re-toggle `Dialog useNativeDialogs: false` ONLY if `vwnt.exe` was restarted** (persists across bridge file-ins). Quick check via `/eval`:
   ```smalltalk
   Dialog usesNativeDialogs
   ```
   Should return `false`. If `true`, set via `/eval`:
   ```smalltalk
   Dialog useNativeDialogs: false
   ```

3. **Push the 12 local commits** (10 from session-4 + 2 already-local session-3 doc commits that I miscounted earlier — actually 12 total after this handoff commit lands). Push is shared-impact → needs explicit "push" verb from user (already given for this session via wrap-session choice; if next session also wants to push, get explicit re-affirmation).

### Bug work (priority order, pick one per session)

4. **Bug #4b full fix — view-tree archaeology** (1-2 hours, well-scoped):
   - Probe `View` / `ScheduledWindow` class hierarchy for the correct child-iteration selector (the standard `subViews` doesn't exist in this VW image — also tried `components`, `subComponents`, `subwidgets`, `children`, `submorphs`, `childrenComponents` in session-4 — none matched)
   - Walk the Help dialog's view tree to find `ActionButton` and `Label` widgets directly
   - Update `extractDialogMessageFrom:` and `extractDialogButtonsFrom:` to fall back to view-tree walk when `namedComponents` is empty
   - Verify on `ExtendedSimpleDialog` (Help) AND a `SimpleDialog` (e.g. trigger `Dialog warn:` via fork) to ensure no regression
   - Bump v0.8.9 → v0.8.10, commit, update Bug #4b status to FIXED

5. **Bug #2 deep dive — silent partialFind: skip** (multi-hour, separate session per session-3 handoff):
   - Instrument `SimpleDialog>>openInterface` to log what `Dialog confirm:` returns after `closeAndUnschedule`
   - Likely fix: simulate the YesButton click instead of `closeAndUnschedule` (Stage 2 from session-3 handoff)
   - Stage 3: guard `/eval` against `Dialog confirm:` / `Dialog request:` bodies (those hang on serve process even with v0.8.8's re-entry guard, because they wait on UI not on dispatcher)
   - Test against `#contractNumber` AND `#surname` (the two affected partialFind: cases per session-3 calibration)

### Carry-overs from session-3 backlog (still pending)

6. **EXPLORATION-PLAN step 3** — 3-deep menu navigation. Pick a leaf like `Party & Contract` → `General reports` → `Contract status` (read-only-sounding, no mutation verb). Verify `walkMenuPath:` handles nested submenus.
7. **EXPLORATION-PLAN step 4** — leaf dispatch catalog across MAS menu tree. Confirm/falsify "all MAS leaves use BlockClosure".

### Production-grade packaging (medium-term, coordinated with user)

8. **Self-test coverage expansion.** Add tests for `/click` (mock Party Search if needed), `/menu`, `/dialogs` (post-Bug-#4b-full-fix), `/value`, `/windows/tree`. Bridge towards CI-runnable suite.
9. **Config externalization.** Env vars for port, token, allowed-hosts. Default file: `.env`.
10. **File-based structured logging** (replace Transcript).
11. **New endpoints:** unified `/state` snapshot, `/reset` (test isolation), `/admin/shutdown`, distinct `/version`.
12. **OpenAPI spec** for the bridge.
13. **Parcel migration + namespace isolation** — gated on tests being in place.
14. **External clients** — `vw-client`, `vw-test-framework`, `vw-mcp-server` npm packages.

---

## Key files

| File | Role |
|---|---|
| [`src/vw-bridge/VWBridge.st`](../src/vw-bridge/VWBridge.st) | **CANONICAL v0.8.9** — all 3 fixes from session-3 (Bug #1 in v0.8.7, Bug #4 in v0.8.6) + session-4 (Bug #5 in v0.8.8, partial Bug #4b in v0.8.9) applied. File via `/eval` `'...VWBridge.st' asFilename fileIn` OR Workspace if bridge is DOWN. |
| [`src/vw-bridge/VWBridge-Test.st`](../src/vw-bridge/VWBridge-Test.st) | SUnit scaffold (7 tests + 6 helpers). Version-locked to v0.8.9 (`testHealthReturnsCurrentVersion` asserts `"version":"0.8.9"`). RUN ONLY from VW Workspace for green results — `/eval`-driven runs all return 400 cleanly in v0.8.8+ but go red on assertions. |
| [`src/vw-bridge/.token`](../src/vw-bridge/.token) | Bridge token (auto-rewritten on every `VWBridge start`). Current value `3959409886993-893140` (mtime should be after the v0.8.9 file-in earlier this session). |
| [`src/vw-bridge/archive/`](../src/vw-bridge/archive/) | Historical 7-file phase chain. Reference only — do NOT file in. |
| [`src/vw-bridge/SESSION-RESUME.md`](../src/vw-bridge/SESSION-RESUME.md) | "FIRST ACTION on resume" guide. Single-file install. |
| [`src/vw-bridge/EXPLORATION-PLAN.md`](../src/vw-bridge/EXPLORATION-PLAN.md) | Step-by-step plan — steps 1+2 done, 3+4 pending. |
| [`knowledge/vw-bridge-known-issues.md`](./vw-bridge-known-issues.md) | Bugs #1-#5 with status. **#1/#4/#5 FIXED, #2 NOT FIXED, #4b PARTIALLY FIXED**. Bug #4b "Investigation" section has full probe-result table from session-4. |
| [`knowledge/vw-party-search.md`](./vw-party-search.md) | PartySearchView usage guide. |
| [`knowledge/vw-bridge-testing.md`](./vw-bridge-testing.md) | Bridge testing methodology + anti-patterns. **Bug #5 anti-pattern softened in session-4** (no longer image-killer, returns 400 cleanly in v0.8.8+). |
| [`knowledge/vw-input-recovery.md`](./vw-input-recovery.md) | What to do if VW input wedges. |
| [`knowledge/vw-dialogs.md`](./vw-dialogs.md) | Dialog/SimpleDialog/closeAndUnschedule mechanics. |
| [`knowledge/vw-eval-cookbook.md`](./vw-eval-cookbook.md) | Useful `/eval` recipes. |
| [`knowledge/smalltalk-gotchas.md`](./smalltalk-gotchas.md) | Chunk format, namespace qualification, ComposedText, etc. |
| [`knowledge/CONSOLIDATION-NOTES.md`](./CONSOLIDATION-NOTES.md) | Audit trail of the v0.8.5 consolidation. |
| [`knowledge/HANDOFF-2026-06-19.md`](./HANDOFF-2026-06-19.md) | Session-3 EOD handoff. Last committed at `a434f69`. |
| [`knowledge/HANDOFF-2026-06-20.md`](./HANDOFF-2026-06-20.md) | **THIS FILE.** Session-4 EOD. |

---

## Important decisions (this session)

- **Bug #5 fix BEFORE commit wave.** The session-3 handoff suggested committing first, then fixing. I inverted: fixed Bug #5 first so the commit could include "verified Bug #5 fix" as part of the wave. Cleaner narrative; net zero on commit count.
- **Stage 1 + Stage 2 BOTH shipped for Bug #5.** The session-3 handoff suggested Stage 1 (substring filter) as a quick mitigation. I went further: also shipped Stage 2 (per-process counter) in the same v0.8.8 because they're complementary (Stage 1 catches obvious foot-guns pre-compile; Stage 2 catches indirect/constructed paths at runtime). The probe-4 test specifically exercised Stage 2 evading Stage 1 to prove the defense-in-depth.
- **Class instance variables for Stage 2 state, lazy-initialized.** Chose `dispatchDepthByProcess` (IdentityDictionary) + `dispatchDepthMutex` (Semaphore) as classInstVars, not instance vars, so the depth tracking is shared across any singleton lifecycle (re-file-in preserves them). Lazy-init in `incrementDispatchDepthFor:` so a fresh class redefinition doesn't need explicit reset.
- **Substring filter uses BOTH `'VWBridge'` AND `'dispatch'`.** Designed to allow the documented workaround #1 (`VWBridge singleton handleWindows` — no 'dispatch' substring). False positives are theoretical for legitimate `/eval` bodies that happen to contain both — none observed in practice.
- **/eval-driven file-in for v0.8.9, not Workspace.** First agent-driven reload since Bug #5 was fixed. Worked cleanly. Going forward this is the default reload path.
- **Bug #4b shipped as PARTIAL fix, not reverted.** The `class` field is concretely useful and backwards-compat. The refactor into helpers is cleaner code. Reverting would lose both. Honest commit message + doc update sets correct expectations.
- **Doc honesty over completeness.** When my v0.8.9 partial fix didn't fully solve Bug #4b, I updated the doc to reflect what was discovered (namedComponents empty, prior diagnosis wrong) and explicitly named what's still deferred. Better than over-claiming "fixed".
- **2-commit split for v0.8.9, not 1.** Code commit (VWBridge.st + VWBridge-Test.st) and docs commit (vw-bridge-known-issues.md) kept separate per session-3 handoff's "3+ files → 2+ commits" convention. Each commit's scope is clean.
- **Session-3 handoff committed as-is** (not updated for session-4 work). Reasoning: it's a HISTORICAL session-3 EOD snapshot; session-4 gets its own new handoff (this file).
- **No PUSH yet.** User explicitly asked for "wrap session: handoff + push" — handoff first (this file), then push after the handoff commits. Push happens at the very end of this session.

---

## Explicit constraints (carry forward)

- **NEVER commit, amend, or push** without explicit user request. Session-4 has had multiple explicit "commit"/"push" verbs; future sessions need fresh authorization.
- **NEVER call `VWBridge singleton dispatch:` from inside `/eval`.** v0.8.8 contains this with 400 returns, but still don't do it intentionally — tests that depend on it will all go red. Use direct handler calls (`VWBridge singleton handleWindows`) or run SUnit from a Workspace.
- **OFF-LIMITS widgets** without explicit OK: `commitWidget`, `loginRpcWidget`, `removeWidget`. **MAS menu leaves with mutation verbs** (add/delete/transfer/commit/change/update/process/submit/upload) need OK before any `/menu/click`.
- **`windowTitle` is CASE-SENSITIVE substring match.** Example matches: `MOMENTUM` matches "MOMENTUM WEALTH - DEVELOPER --- Session Number: 4", `Party Search` matches "Party Search", `Help` matches "Help".
- **For Workspace file-in (bridge DOWN scenario):** `'path' asFilename fileIn.`, NOT Launcher → File Browser → File In. Chunk-parser quirks fail there.
- **For `/eval` file-in (bridge UP scenario, new in v0.8.8+):**
  ```powershell
  $tok = (Get-Content "C:\Users\ammaganyane\tm\tm-context\src\vw-bridge\.token").Trim()
  curl.exe -sS -X POST -H "Authorization: Bearer $tok" -H "Content-Type: text/plain" --data "'C:\Users\ammaganyane\tm\tm-context\src\vw-bridge\VWBridge.st' asFilename fileIn" http://127.0.0.1:9876/eval
  ```
  Token rotates on every reload — re-read `.token` afterward.
- **`$'` character literal is unreliable** in chunk file-ins. Use `(Core.Character value: 39)`.
- **GBS namespace qualification** — `Root.Smalltalk`, `Core.String`, `Core.Character`, `Core.Error`, `Core.Array`, `Core.Integer`, `Core.OrderedCollection`, `Core.Dictionary`.
- **`/eval` runs on bridge serve process**, NOT UI process. UI-touching `/eval` needs `ScheduledControllers activeControllerProcess interruptWith:` wrapping. AND must not re-invoke `bridge dispatch:` (v0.8.8 Stage 2 catches this with 400 but you still don't want to write it).
- **`Dialog useNativeDialogs: false` setting:** persists across bridge file-in, does NOT persist across `vwnt.exe` restart. Re-toggle on every image restart. `/eval` works for this (body has 'Dialog' but not 'VWBridge' or 'dispatch'; both Stage 1 + Stage 2 transparent).
- **PowerShell + `curl.exe` >> WSL bash for HTTP probes.** Multi-layer quote escaping through PowerShell → WSL → bash → curl is hopeless when bodies contain single quotes. Use Windows-native `curl.exe` with file-based `--data-binary @path` for bodies with quotes. User explicitly suggested this mid-session; it works cleanly.

---

## Context for continuation (read this before resuming)

- **Bridge is UP at session-4 EOD.** Verified via `curl.exe -sS http://127.0.0.1:9876/health` returning `{"status":"ok","version":"0.8.9"}`. Token in `.token`. No restart needed unless something has changed since.
- **`Dialog useNativeDialogs: false` is set** (set during session-4 Bug #4b verification). Persists unless `vwnt.exe` was restarted. Confirm via `/eval` `Dialog usesNativeDialogs`.
- **11 commits LOCAL ONLY** at the moment this handoff is being written. Once this handoff is committed (commit 12), then pushed. Push needs explicit OK ON THE NEXT SESSION if not done in this one — user already authorized for this session via "wrap session: handoff + push".
- **Bug #4b is HALF-DONE.** The doc is honest about what's shipped (class field, defensive helpers) vs what's deferred (message + buttons for ExtendedSimpleDialog, needs view-tree walking). Pick this up only if you want a 1-2 hour focused session on VW view-tree archaeology. Otherwise Bug #2 is the next big-value bug.
- **For ANY bridge change in v0.8.10+**: edit [`VWBridge.st`](../src/vw-bridge/VWBridge.st), reload via `/eval` file-in (no need for Workspace anymore), bump version at 4 sites (header, dispatcher comment, doDispatch comment, /health body) + the matching `testHealthReturnsCurrentVersion` assertion.
- **For ANY new SUnit tests**: add to [`VWBridge-Test.st`](../src/vw-bridge/VWBridge-Test.st). Prefer calling handler methods directly (e.g. `bridge handleWindows`) over `bridge dispatch: ...` — the latter goes through Stage 2's per-process counter; while the workspace-run path is transparent (depth starts at 0), it's still cleaner architecturally.
- **Production-grade packaging is on the roadmap, not critical path.** Bug fixes + SUnit coverage come FIRST. The session-3 handoff's "internal-grade ~2-3 weeks, external ~+4-8 weeks" assessment still holds.

---

## To continue in a new session

1. Press `n` in OpenCode TUI to open a new session, or run `opencode` in a new terminal.
2. Paste this entire file as your first message.
3. Add your request — pick one based on focus:
   - "Continue from handoff above. Smoke-test bridge, then pick up Bug #4b view-tree archaeology (1-2 hours focused)." — closes out the Bug #4b deferred work
   - "Continue from handoff above. Smoke-test bridge, then start Bug #2 deep dive — instrument SimpleDialog>>openInterface to confirm what Dialog confirm: returns after closeAndUnschedule." — the gnarly multi-hour deep-dive bug
   - "Continue from handoff above. Skip bug work, jump to EXPLORATION-PLAN step 3 (3-deep menu navigation) and step 4 (leaf dispatch catalog)." — carry-over backlog from session-2
   - "Continue from handoff above. Start production-grade packaging work — config externalization (env vars for port/token), file-based structured logging, /state + /reset endpoints." — pivots to the medium-term packaging track

The new session will have full context to continue seamlessly.
